[package]
name = "medmarket"
version = "0.0.1"
published-at = "0x0"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

[addresses]
medmarket = "0x0"  # Quan trọng: Phải khớp với 'module medmarket::...' trong code